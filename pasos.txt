Se tendrá un invernadero con plantas separadas por posicion, siendo las posiciones del 0 al 100.
Cada posicion esta asociado a una planta la cual tiene que tener un cierto porcentaje de humedad,
temperatura, y luminozidad


primero se hizo el pull de la imagen de rabbit

docker pull rabbitmq:3-management

Now let’s stand it up. We’ll map port 15672 for the management web app and port 5672 for the message broker.

docker run --rm -it -p 15672:15672 -p 5672:5672 rabbitmq:3-management


tambien se instalo pika con pip: pip install pika.
